class HomeBase {
    // coords
    public var x : Int;
    public var y : Int;

    // TODO: other stuff, like hitpoint and attack abilities

    public function new(x : Int, y : Int) {
        this.x = x;
        this.y = y;
    }
}